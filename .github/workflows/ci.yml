# name: CI

# on:
#   push:
#     branches: [ main, dev ]
#   pull_request:
#     branches: [ main, dev ]
#   workflow_dispatch:

# jobs:
#   server-tests:
#     name: Server tests
#     runs-on: [self-hosted, private-runner, linux, x64]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'

#       - name: Use self-hosted runner's MongoDB
#         # This workflow expects the self-hosted runner to have MongoDB available
#         # and listening on 127.0.0.1:27017. The runner should also have npm/node
#         # installed. If you need help registering a runner, see the README below.
#         run: |
#           echo "Assuming self-hosted runner provides mongod on 127.0.0.1:27017"
#           echo "If your runner uses a different host/port, set TEST_MONGO_URI in repository secrets or runner env."

#       - name: Install server deps
#         run: |
#           cd server
#           npm install

#       - name: Run server unit tests
#         run: |
#           cd server
#           npm test

#   # Frontend tests commented out until frontend is finished. Uncomment when ready.
#   # client-unit:
#   #   name: Client unit tests
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - uses: actions/checkout@v4
#   #
#   #     - name: Setup Node.js
#   #       uses: actions/setup-node@v4
#   #       with:
#   #         node-version: '20'
#   #         cache: 'npm'
#   #
#   #     - name: Install client deps
#   #       run: |
#   #         cd client
#   #         npm ci
#   #
#   #     - name: Run client unit tests (Vitest)
#   #       run: |
#   #         cd client
#   #         npm run test:unit -- --run
#   #
#   # client-e2e:
#   #   name: Client E2E (Playwright)
#   #   runs-on: ubuntu-latest
#   #   needs: client-unit
#   #   steps:
#   #     - uses: actions/checkout@v4
#   #
#   #     - name: Setup Node.js
#   #       uses: actions/setup-node@v4
#   #       with:
#   #         node-version: '20'
#   #         cache: 'npm'
#   #
#   #     - name: Install client deps
#   #       run: |
#   #         cd client
#   #         npm ci
#   #
#   #     - name: Install Playwright browsers
#   #       run: |
#   #         cd client
#   #         npx playwright install --with-deps
#   #
#   #     - name: Start dev server in background
#   #       run: |
#   #         cd client
#   #         npm run dev --silent &
#   #         npx wait-on http://127.0.0.1:5173
#   #
#   #     - name: Run Playwright E2E tests
#   #       run: |
#   #         cd client
#   #         npm run test:e2e -- --hea dless