name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:

jobs:
  server-tests:
    name: Server tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Prepare environment
        # Tests use an in-memory MongoDB (mongodb-memory-server) via the test setup
        # file, so no external MongoDB is required. Ensure node/npm are available.
        run: |
          node --version
          npm --version

      - name: Install MongoDB (server) and start service
        # Install official MongoDB package so tests can connect to a local mongod
        # without requiring mongodb-memory-server to spawn a binary needing old openssl.
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y gnupg ca-certificates curl
          curl -fsSL https://www.mongodb.org/static/pgp/server-6.0.asc | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-6.gpg
          echo "deb [signed-by=/usr/share/keyrings/mongodb-server-6.gpg] https://repo.mongodb.org/apt/ubuntu $(lsb_release -sc)/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
          sudo apt-get update
          sudo apt-get install -y mongodb-org
          # Start mongod
          sudo systemctl enable --now mongod || sudo service mongod start || true
          # wait briefly for mongod to be ready
          for i in {1..10}; do
            nc -z 127.0.0.1 27017 && break || sleep 1
          done


      - name: Install server deps
        run: |
          cd server
          npm install

      - name: Run server unit tests
        run: |
          cd server
          npm test
        env:
          TEST_MONGO_URI: 'mongodb://127.0.0.1:27017'

  # Frontend tests commented out until frontend is finished. Uncomment when ready.
  # client-unit:
  #   name: Client unit tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: Install client deps
  #       run: |
  #         cd client
  #         npm ci
  #
  #     - name: Run client unit tests (Vitest)
  #       run: |
  #         cd client
  #         npm run test:unit -- --run
  #
  # client-e2e:
  #   name: Client E2E (Playwright)
  #   runs-on: ubuntu-latest
  #   needs: client-unit
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: Install client deps
  #       run: |
  #         cd client
  #         npm ci
  #
  #     - name: Install Playwright browsers
  #       run: |
  #         cd client
  #         npx playwright install --with-deps
  #
  #     - name: Start dev server in background
  #       run: |
  #         cd client
  #         npm run dev --silent &
  #         npx wait-on http://127.0.0.1:5173
  #
  #     - name: Run Playwright E2E tests
  #       run: |
  #         cd client
  #         npm run test:e2e -- --hea dless