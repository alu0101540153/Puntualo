// Enums reutilizables para modelos
export enum RatedStatus {
  COMPLETED = 'completed',
  WATCHING = 'watching'
}

export enum ItemType {
  MOVIE = 'movie',
  SERIES = 'series',
  BOOK = 'book'
}

export default RatedStatus;
